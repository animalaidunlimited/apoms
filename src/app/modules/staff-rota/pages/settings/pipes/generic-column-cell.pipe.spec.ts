/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GenericColumnCellPipe } from './generic-column-cell.pipe';

describe('Pipe: GenericColumnCelle', () => {
  it('create an instance', () => {
    let pipe = new GenericColumnCellPipe();
    expect(pipe).toBeTruthy();
  });
});
